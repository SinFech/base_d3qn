# Legacy code

This folder contains the original notebook-derived `src/` code from the baseline.
It is kept for reference only. Use the `rl/` modules and `scripts/` entrypoints
for training and evaluation.
